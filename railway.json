{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd telo-mini-fronted && npm install && npm run build && mkdir -p ../telo-mini-backend/public/build && cp -r build/* ../telo-mini-backend/public/"
  },
  "deploy": {
    "startCommand": "cd telo-mini-backend && composer install && php artisan serve --host=0.0.0.0 --port=$PORT",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
